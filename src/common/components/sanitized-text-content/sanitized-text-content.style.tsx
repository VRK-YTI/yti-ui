import styled from 'styled-components';
import { ExternalLink, Link } from 'suomifi-ui-components';

export const SuomiInternalLink = styled(Link)`
  font-size: inherit;
`;

export const SuomiExternalLink = styled(ExternalLink)`
  font-size: inherit;
`;
