export interface ServiceConfiguration {
  codeListUrl: string;  
  dataModelUrl: string;
  commentsUrl: string;
  groupmanagementUrl: string;
  env: string;
  restrictFilterOptions: boolean;
}
